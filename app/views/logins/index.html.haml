=form_tag logins_path, :method=>'get', :id=> "logins_search" do
  .input-append
    = text_field_tag :search, params[:search], :id=>"appendedInputButton"
    = submit_tag "Search", :name => nil, :class=>"btn"
  #logins_all
    = render 'logins'
